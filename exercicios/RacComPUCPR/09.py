
for i in range(5): #tradução = para i no intervalo / range = intervalo

    print(i)

    #No código acima, a “esteira rolante” criada terá os números 0, 1, 2, 3 e 4. Note que ela começa em 0 e para antes de chegar em 5. Quando informamos só um número dentro do range() ele entende automaticamente que o início é 0, o fim é o número 5, e o passo é 1 (ou seja: a contagem será de um em um).

